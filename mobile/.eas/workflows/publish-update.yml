name: Publish update
on:
  push:
    branches: ['*']
jobs:
  update:
    name: Update
    if: ${{ contains(github.commit_message || '', '[ios]') }}
    type: update
    params:
      branch: ${{ github.ref_name || 'test' }}
